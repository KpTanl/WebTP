# TP 考试参考资料大纲

## 1. 课程总览与考试思路

- 课程内容主线
- 各 TP 在整套项目里的位置
- 综合题常见结构
- 推荐做题顺序
- 时间分配建议

## 2. 从 VS Code 创建 Spring Boot 项目

- 使用 Spring Initializr 创建项目
- Group / Artifact / Name 怎么填
- 选择 Java 版本
- 选择 Maven
- 选择常用依赖
- 项目导入 VS Code
- 首次启动项目
- 常见目录结构说明

## 3. `pom.xml` 常用依赖怎么选

- Spring Boot Web
- Thymeleaf
- Spring Data JPA
- SQLite JDBC
- Hibernate SQLite Dialect
- Spring Security
- `spring-security-crypto`
- DevTools
- Test 依赖

本章要整理：

- 哪些依赖是 Spring Initializr 可直接勾选的
- 哪些依赖需要手动补到 `pom.xml`
- TP 里各项目实际用了哪些依赖

## 4. `application.properties` 常用配置模板

- 应用名
- 端口
- SQLite 数据源配置
- JDBC Driver 配置
- Hibernate Dialect 配置
- `ddl-auto` 配置
- `show-sql` 配置
- Thymeleaf 缓存配置
- 默认测试账号配置
- 外部 API 配置

## 5. Spring MVC 基础

- MVC 是什么
- Controller 的作用
- Model 的作用
- View 的作用
- `@Controller` 和 `@RestController` 区别
- 请求进入项目后的处理流程

## 6. 路由与参数接收

- `@RequestMapping`
- `@GetMapping`
- `@PostMapping`
- `@RequestParam`
- `@PathVariable`
- 查询参数和路径参数的区别
- 什么时候用 GET
- 什么时候用 POST

## 7. 表单提交与页面跳转

- HTML 表单基础
- `action` / `method`
- 表单字段命名与后端参数对应
- 提交后返回模板
- `redirect:`
- `forward:`
- 删除、修改、搜索的典型表单写法

## 8. Thymeleaf 模板语法速查

- `th:text`
- `th:each`
- `th:if`
- `th:unless`
- `th:href`
- `th:action`
- `th:value`
- 表单与 CSRF
- 页面中循环表格
- 页面中条件显示按钮

## 9. Model / DTO / Entity 分别是什么

- `Model` 用来给页面传数据
- DTO 用来接 API 数据或做数据传输
- Entity 用来映射数据库表
- 三者的区别
- 三者在综合题里的位置

## 10. SQLite 与 JPA 基础配置

- SQLite 是什么
- JPA 是什么
- Spring Data JPA 是什么
- 如何连接 SQLite
- 如何让 Hibernate 使用 SQLite Dialect
- 启动时自动建表 / 更新表

## 11. 实体类怎么写

- `@Entity`
- `@Table`
- `@Id`
- `@GeneratedValue`
- `@Column`
- 主键字段怎么写
- 普通字段怎么写
- 唯一约束怎么写

## 12. 一对多、多对一、多对多怎么建

- `@OneToMany`
- `@ManyToOne`
- `@ManyToMany`
- `@JoinColumn`
- `@JoinTable`
- 谁是关系拥有方
- 连接表怎么理解
- TP4 和 TP6 中的实际例子

## 13. Repository 常用函数命名法

- `findById`
- `findAll`
- `save`
- `deleteById`
- `findByEmail`
- `existsByEmail`
- `findByTitleContainingIgnoreCase`
- `findByOrganizer_Id`
- `findByParticipants_Id`
- `findByStatus_CodeStatut`

本章重点：

- Spring Data JPA 方法名如何自动生成查询
- 按字段查询
- 按关联对象字段查询
- 模糊查询
- 是否存在查询

## 14. Service 层怎么组织业务逻辑

- 为什么要有 Service 层
- Controller 不该写太多什么逻辑
- Repository 和 Service 的分工
- 参数校验
- 默认值处理
- 更新时如何保留旧值
- 密码加密应该放哪里

## 15. CRUD 标准模板

- 新增模板
- 查询模板
- 修改模板
- 删除模板
- 搜索模板
- 表单页面 + Controller + Service + Repository 的完整配套结构

## 16. 登录、Session、Spring Security 基础

- Session 是什么
- Session 登录怎么做
- Spring Security 登录怎么做
- `UserDetailsService`
- `SecurityFilterChain`
- 登录页配置
- 哪些页面公开
- 哪些页面必须登录
- `Principal` 怎么拿当前用户

## 17. 密码加密与 `BCryptPasswordEncoder`

- 为什么密码不能明文存储
- `PasswordEncoder`
- `BCryptPasswordEncoder`
- 注册时加密
- 登录时 `matches`
- 为什么不能重复加密已加密密码
- 如何判断字符串像不像 bcrypt hash

## 18. 外部 API 调用

- 什么情况下需要 DTO
- `RestTemplate` 怎么用
- URL 怎么拼
- 查询参数怎么传
- API Key 放哪里
- 调用失败时怎么排查

## 19. `JSON -> DTO -> Entity -> 数据库` 的完整链路

- API 返回 JSON
- JSON 映射到 DTO
- DTO 转成 Entity
- Entity 保存到数据库
- 后续从数据库重新读取
- 用户数据与 API 数据怎么结合

## 20. 前端模板展示数据库数据

- Controller 从数据库取数据
- 放进 `Model`
- 模板页面循环渲染
- 明细页怎么写
- 列表页怎么写
- 条件按钮怎么写

## 21. 综合题完整骨架

- 项目创建
- 依赖配置
- 配置文件
- Entity
- Repository
- Service
- Controller
- Template
- 登录
- 加密
- API
- 入库
- 页面展示

这一章建议最后做成：

- 一个最小可运行项目骨架
- 一个综合题答题顺序模板

## 22. 常用注解速查表

- `@SpringBootApplication`
- `@Controller`
- `@RestController`
- `@Service`
- `@Repository`
- `@Configuration`
- `@Bean`
- `@GetMapping`
- `@PostMapping`
- `@RequestParam`
- `@PathVariable`
- `@ModelAttribute`
- `@Entity`
- `@Table`
- `@Id`
- `@GeneratedValue`
- `@Column`
- `@OneToMany`
- `@ManyToOne`
- `@ManyToMany`
- `@JoinColumn`
- `@JoinTable`

## 23. 常用函数模板汇总

- Controller 模板
- 表单接收模板
- 列表展示模板
- 详情页模板
- 新增模板
- 修改模板
- 删除模板
- 搜索模板
- 注册模板
- 登录模板
- 密码加密模板
- API 调用模板
- DTO 转 Entity 模板
- Repository 查询模板

## 24. 考试高频错误与检查清单

- 路由写错
- 表单字段名和后端参数名不一致
- 忘记加 `Model` 数据
- 模板变量名写错
- `redirect:` 和直接返回模板混淆
- Entity 关系写反
- 数据库配置漏写
- 忘记加依赖
- 密码明文比较
- 登录后拿不到当前用户
- API Key 配置错误
- 数据没有真正保存到数据库

## 25. 常见报错

- Whitelabel Error Page
- 404 Not Found
- 405 Method Not Allowed
- Template not found
- Failed to configure a DataSource
- Cannot determine Dialect
- No suitable driver
- Table not found
- Column not found
- `UsernameNotFoundException`
- `Bad credentials`
- `NullPointerException`
- `Optional` 取值异常
- `Could not resolve placeholder`
- JSON 映射失败
- API 调用失败

每个报错建议整理成固定格式：

- 报错长什么样
- 一般原因
- 怎么检查
- 怎么改

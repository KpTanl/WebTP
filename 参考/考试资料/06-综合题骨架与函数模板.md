# 06 综合题骨架与函数模板

这一章不讲概念，直接按“考试题怎么落地”来写。

假设题目是：

- 调一个电影 API
- 把电影保存到 SQLite
- 做登录
- 用户可以收藏电影
- 页面用 Thymeleaf 展示

## 21. 综合题完整骨架

### 21.1 文件结构

```text
src/main/java/fr/miage/examdemo
├── ExamdemoApplication.java
├── config
│   ├── SecurityBeansConfig.java
│   ├── DataInitializer.java
│   └── HttpClientConfig.java
├── controller
│   ├── AuthController.java
│   ├── MemberSpaceController.java
│   ├── StoredMovieController.java
│   └── MovieRestController.java
├── dto
│   ├── RemoteMovieDTO.java
│   └── RemoteMoviePageDTO.java
├── entity
│   ├── AppMemberEntity.java
│   ├── StoredMovieEntity.java
│   └── MemberMoviePreferenceEntity.java
├── repository
│   ├── AppMemberRepository.java
│   ├── StoredMovieRepository.java
│   └── MemberMoviePreferenceRepository.java
└── service
    ├── AppMemberService.java
    ├── MemberMoviePreferenceService.java
    ├── RemoteMovieService.java
    └── StoredMovieService.java

src/main/resources
├── application.properties
├── templates
│   ├── login.html
│   ├── register.html
│   ├── movie-list.html
│   ├── movie-detail.html
│   ├── stored-movie-list.html
│   ├── my-favorites.html
│   └── my-watchlist.html
└── static
    ├── css
    └── js
```

### 21.2 推荐答题顺序

1. 建项目
2. 补依赖
3. 写 `application.properties`
4. 写 3 个实体类
5. 写 3 个 Repository
6. 写 API Service
7. 写数据库 Service
8. 写登录、密码加密、Session
9. 写页面 Controller
10. 写模板
11. 最后补收藏逻辑

### 21.3 启动类

```java
@SpringBootApplication
public class ExamdemoApplication {

    public static void main(String[] args) {
        SpringApplication.run(ExamdemoApplication.class, args);
    }
}
```

### 补充：`pom.xml` 常用依赖

这一套优先按 `TP4` 老师要求来写：密码用 `BCrypt` 哈希，登录状态放在 `HttpSession`。

```xml
<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>

    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>

    <dependency>
        <groupId>org.springframework.security</groupId>
        <artifactId>spring-security-crypto</artifactId>
    </dependency>

    <dependency>
        <groupId>org.xerial</groupId>
        <artifactId>sqlite-jdbc</artifactId>
    </dependency>

    <dependency>
        <groupId>org.hibernate.orm</groupId>
        <artifactId>hibernate-community-dialects</artifactId>
    </dependency>

    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-test</artifactId>
        <scope>test</scope>
    </dependency>
</dependencies>
```

如果题目明确要求 `Spring Security` 登录保护，再额外补：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>
```

### 补充：`application.properties` 最小模板

```properties
spring.application.name=examdemo
server.port=8080

spring.datasource.url=jdbc:sqlite:db/examdemo.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.thymeleaf.cache=false

movie.api.base-url=https://api.themoviedb.org/3
movie.api-key=YOUR_API_KEY

app.security.default-user.email=admin@test.com
app.security.default-user.password=admin123
```

补充说明：

- SQLite 常见报错大多都和这几行配置有关
- 开卷考试时先把数据源、方言、`ddl-auto` 补齐，再去写实体
- `movie.api-key` 题目如果给了外部 API，通常都要自己补到配置里

### 21.4 三个核心实体

#### 用户

```java
@Entity
@Table(name = "app_member")
public class AppMemberEntity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false)
    private String fullName;

    @Column(nullable = false, unique = true)
    private String email;

    @Column(nullable = false)
    private String passwordHash;

    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public String getFullName() { return fullName; }
    public void setFullName(String fullName) { this.fullName = fullName; }
    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
    public String getPasswordHash() { return passwordHash; }
    public void setPasswordHash(String passwordHash) { this.passwordHash = passwordHash; }
}
```

#### 电影

```java
@Entity
@Table(name = "stored_movie")
public class StoredMovieEntity {

    @Id
    private Long id;

    @Column(nullable = false)
    private String title;

    private String releaseDate;
    private Double voteAverage;

    @Column(length = 3000)
    private String overview;

    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public String getTitle() { return title; }
    public void setTitle(String title) { this.title = title; }
    public String getReleaseDate() { return releaseDate; }
    public void setReleaseDate(String releaseDate) { this.releaseDate = releaseDate; }
    public Double getVoteAverage() { return voteAverage; }
    public void setVoteAverage(Double voteAverage) { this.voteAverage = voteAverage; }
    public String getOverview() { return overview; }
    public void setOverview(String overview) { this.overview = overview; }
}
```

#### 用户-电影关系

```java
@Entity
@Table(
        name = "member_movie_preference",
        uniqueConstraints = @UniqueConstraint(columnNames = {"member_id", "movie_id"})
)
public class MemberMoviePreferenceEntity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne
    @JoinColumn(name = "member_id", nullable = false)
    private AppMemberEntity appMember;

    @ManyToOne
    @JoinColumn(name = "movie_id", nullable = false)
    private StoredMovieEntity storedMovie;

    @Column(nullable = false)
    private Boolean favorite;

    @Column(nullable = false)
    private Boolean watchlist;

    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public AppMemberEntity getAppMember() { return appMember; }
    public void setAppMember(AppMemberEntity appMember) { this.appMember = appMember; }
    public StoredMovieEntity getStoredMovie() { return storedMovie; }
    public void setStoredMovie(StoredMovieEntity storedMovie) { this.storedMovie = storedMovie; }
    public Boolean getFavorite() { return favorite; }
    public void setFavorite(Boolean favorite) { this.favorite = favorite; }
    public Boolean getWatchlist() { return watchlist; }
    public void setWatchlist(Boolean watchlist) { this.watchlist = watchlist; }
}
```

### 21.5 Repository 最小模板

```java
public interface AppMemberRepository extends JpaRepository<AppMemberEntity, Long> {
    AppMemberEntity findByEmail(String email);
    boolean existsByEmail(String email);
}
```

```java
public interface StoredMovieRepository extends JpaRepository<StoredMovieEntity, Long> {
    List<StoredMovieEntity> findByTitleContainingIgnoreCase(String title);
    boolean existsById(Long id);
}
```

```java
public interface MemberMoviePreferenceRepository
        extends JpaRepository<MemberMoviePreferenceEntity, Long> {

    MemberMoviePreferenceEntity findByAppMember_IdAndStoredMovie_Id(Long memberId, Long movieId);

    List<MemberMoviePreferenceEntity> findByAppMember_IdAndFavoriteTrue(Long memberId);

    List<MemberMoviePreferenceEntity> findByAppMember_IdAndWatchlistTrue(Long memberId);
}
```

### 21.6 DTO 最小模板

```java
public class RemoteMovieDTO {
    private Long id;
    private String title;
    private String release_date;
    private Double vote_average;
    private String overview;

    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public String getTitle() { return title; }
    public void setTitle(String title) { this.title = title; }
    public String getRelease_date() { return release_date; }
    public void setRelease_date(String release_date) { this.release_date = release_date; }
    public Double getVote_average() { return vote_average; }
    public void setVote_average(Double vote_average) { this.vote_average = vote_average; }
    public String getOverview() { return overview; }
    public void setOverview(String overview) { this.overview = overview; }
}
```

```java
public class RemoteMoviePageDTO {
    private Integer page;
    private Integer total_pages;
    private List<RemoteMovieDTO> results;

    public Integer getPage() { return page; }
    public void setPage(Integer page) { this.page = page; }
    public Integer getTotal_pages() { return total_pages; }
    public void setTotal_pages(Integer total_pages) { this.total_pages = total_pages; }
    public List<RemoteMovieDTO> getResults() { return results; }
    public void setResults(List<RemoteMovieDTO> results) { this.results = results; }
}
```

### 21.7 配置类

#### `RestTemplate`

```java
@Configuration
public class HttpClientConfig {

    @Bean
    public RestTemplate restTemplate() {
        return new RestTemplate();
    }
}
```

#### 密码加密

```java
@Configuration
public class SecurityBeansConfig {

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

### 21.8 API Service

```java
@Service
public class RemoteMovieService {

    private final RestTemplate restTemplate;

    @Value("${movie.api.base-url}")
    private String movieApiBaseUrl;

    @Value("${movie.api-key}")
    private String movieApiKey;

    public RemoteMovieService(RestTemplate restTemplate) {
        this.restTemplate = restTemplate;
    }

    public RemoteMoviePageDTO getPopularMovies(int pageNumber) {
        String url = movieApiBaseUrl
                + "/movie/popular?page=" + pageNumber
                + "&api_key=" + movieApiKey
                + "&language=fr-FR";
        return restTemplate.getForObject(url, RemoteMoviePageDTO.class);
    }

    public RemoteMovieDTO getMovieById(Long movieId) {
        String url = movieApiBaseUrl
                + "/movie/" + movieId
                + "?api_key=" + movieApiKey
                + "&language=fr-FR";
        return restTemplate.getForObject(url, RemoteMovieDTO.class);
    }
}
```

### 21.9 数据库存储 Service

```java
@Service
public class StoredMovieService {

    private final StoredMovieRepository storedMovieRepository;
    private final RemoteMovieService remoteMovieService;

    public StoredMovieService(
            StoredMovieRepository storedMovieRepository,
            RemoteMovieService remoteMovieService) {
        this.storedMovieRepository = storedMovieRepository;
        this.remoteMovieService = remoteMovieService;
    }

    public List<StoredMovieEntity> findAll() {
        return storedMovieRepository.findAll();
    }

    public List<StoredMovieEntity> searchByTitle(String keyword) {
        if (keyword == null || keyword.isBlank()) {
            return storedMovieRepository.findAll();
        }
        return storedMovieRepository.findByTitleContainingIgnoreCase(keyword);
    }

    public StoredMovieEntity findById(Long movieId) {
        return storedMovieRepository.findById(movieId).orElse(null);
    }

    public StoredMovieEntity saveMovieFromApi(Long movieId) {
        StoredMovieEntity existingMovie = findById(movieId);
        if (existingMovie != null) {
            // 数据库里已经有这部电影，就直接复用
            // 避免同一个 movieId 重复入库
            return existingMovie;
        }

        // 先去远程 API 取电影明细
        RemoteMovieDTO remoteMovieDTO = remoteMovieService.getMovieById(movieId);
        // 再把 DTO 转成我们自己的数据库实体
        StoredMovieEntity storedMovie = convertToEntity(remoteMovieDTO);
        // save(...) 才是把电影真正写入 SQLite
        return storedMovieRepository.save(storedMovie);
    }

    private StoredMovieEntity convertToEntity(RemoteMovieDTO remoteMovieDTO) {
        StoredMovieEntity storedMovie = new StoredMovieEntity();
        storedMovie.setId(remoteMovieDTO.getId());
        storedMovie.setTitle(remoteMovieDTO.getTitle());
        storedMovie.setReleaseDate(remoteMovieDTO.getRelease_date());
        storedMovie.setVoteAverage(remoteMovieDTO.getVote_average());
        storedMovie.setOverview(remoteMovieDTO.getOverview());
        return storedMovie;
    }
}
```

### 21.10 用户 Service

```java
@Service
public class AppMemberService {

    private final AppMemberRepository appMemberRepository;
    private final PasswordEncoder passwordEncoder;

    public AppMemberService(
            AppMemberRepository appMemberRepository,
            PasswordEncoder passwordEncoder) {
        this.appMemberRepository = appMemberRepository;
        this.passwordEncoder = passwordEncoder;
    }

    public AppMemberEntity findByEmail(String email) {
        return appMemberRepository.findByEmail(email);
    }

    public AppMemberEntity findById(Long memberId) {
        if (memberId == null) {
            return null;
        }
        return appMemberRepository.findById(memberId).orElse(null);
    }

    public AppMemberEntity findRequiredByEmail(String email) {
        AppMemberEntity appMember = appMemberRepository.findByEmail(email);
        if (appMember == null) {
            throw new IllegalArgumentException("Utilisateur introuvable");
        }
        return appMember;
    }

    public AppMemberEntity authenticate(String email, String rawPassword) {
        AppMemberEntity appMember = appMemberRepository.findByEmail(email);
        if (appMember == null || rawPassword == null || rawPassword.isBlank()) {
            return null;
        }
        if (!passwordEncoder.matches(rawPassword, appMember.getPasswordHash())) {
            return null;
        }
        return appMember;
    }

    public AppMemberEntity registerMember(String fullName, String email, String rawPassword) {
        if (fullName == null || fullName.isBlank()) {
            throw new IllegalArgumentException("Nom obligatoire");
        }
        if (email == null || email.isBlank()) {
            throw new IllegalArgumentException("Email obligatoire");
        }
        if (rawPassword == null || rawPassword.isBlank()) {
            throw new IllegalArgumentException("Mot de passe obligatoire");
        }
        if (appMemberRepository.existsByEmail(email)) {
            throw new IllegalArgumentException("Email deja utilise");
        }

        // 先在内存里组装一个新的用户实体
        AppMemberEntity appMember = new AppMemberEntity();
        appMember.setFullName(fullName);
        appMember.setEmail(email);
        // 注意：数据库保存的是 hash，不是明文密码
        appMember.setPasswordHash(passwordEncoder.encode(rawPassword));
        // save(...) 才会把用户真正插入数据库
        return appMemberRepository.save(appMember);
    }
}
```

### 21.11 收藏 Service

```java
@Service
public class MemberMoviePreferenceService {

    private final MemberMoviePreferenceRepository memberMoviePreferenceRepository;
    private final AppMemberRepository appMemberRepository;
    private final StoredMovieService storedMovieService;

    public MemberMoviePreferenceService(
            MemberMoviePreferenceRepository memberMoviePreferenceRepository,
            AppMemberRepository appMemberRepository,
            StoredMovieService storedMovieService) {
        this.memberMoviePreferenceRepository = memberMoviePreferenceRepository;
        this.appMemberRepository = appMemberRepository;
        this.storedMovieService = storedMovieService;
    }

    public void addFavorite(String email, Long movieId) {
        MemberMoviePreferenceEntity preference = getOrCreatePreference(email, movieId);
        // 这里只是修改内存里的关系对象
        preference.setFavorite(true);
        // save(...) 才把关系表中的 favorite 状态写回数据库
        memberMoviePreferenceRepository.save(preference);
    }

    public void removeFavorite(String email, Long movieId) {
        MemberMoviePreferenceEntity preference = findExistingPreference(email, movieId);
        if (preference == null) {
            return;
        }
        preference.setFavorite(false);
        memberMoviePreferenceRepository.save(preference);
    }

    public void addWatchlist(String email, Long movieId) {
        MemberMoviePreferenceEntity preference = getOrCreatePreference(email, movieId);
        // 如果关系已存在，就更新；不存在就新建后再入库
        preference.setWatchlist(true);
        memberMoviePreferenceRepository.save(preference);
    }

    public void removeWatchlist(String email, Long movieId) {
        MemberMoviePreferenceEntity preference = findExistingPreference(email, movieId);
        if (preference == null) {
            return;
        }
        preference.setWatchlist(false);
        memberMoviePreferenceRepository.save(preference);
    }

    public List<MemberMoviePreferenceEntity> findFavoritePreferences(String email) {
        AppMemberEntity appMember = appMemberRepository.findByEmail(email);
        return memberMoviePreferenceRepository.findByAppMember_IdAndFavoriteTrue(appMember.getId());
    }

    public List<MemberMoviePreferenceEntity> findWatchlistPreferences(String email) {
        AppMemberEntity appMember = appMemberRepository.findByEmail(email);
        return memberMoviePreferenceRepository.findByAppMember_IdAndWatchlistTrue(appMember.getId());
    }

    private MemberMoviePreferenceEntity getOrCreatePreference(String email, Long movieId) {
        // 先根据邮箱找到当前用户
        AppMemberEntity appMember = appMemberRepository.findByEmail(email);
        // 再确保电影表里已经有这部电影，没有就先入库
        StoredMovieEntity storedMovie = storedMovieService.saveMovieFromApi(movieId);

        // 关系表按 “用户 + 电影” 查是否已有记录
        MemberMoviePreferenceEntity preference =
                memberMoviePreferenceRepository.findByAppMember_IdAndStoredMovie_Id(
                        appMember.getId(), storedMovie.getId());

        if (preference == null) {
            // 数据库里还没有这条关系，就先 new 一个关系实体
            preference = new MemberMoviePreferenceEntity();
            preference.setAppMember(appMember);
            preference.setStoredMovie(storedMovie);
            preference.setFavorite(false);
            preference.setWatchlist(false);
        }
        // 这里只返回对象，真正入库发生在外层的 save(...)
        return preference;
    }

    private MemberMoviePreferenceEntity findExistingPreference(String email, Long movieId) {
        AppMemberEntity appMember = appMemberRepository.findByEmail(email);
        if (appMember == null) {
            return null;
        }
        return memberMoviePreferenceRepository.findByAppMember_IdAndStoredMovie_Id(
                appMember.getId(), movieId);
    }
}
```

### 21.12 登录 Controller

```java
@Controller
public class AuthController {

    public static final String SESSION_USER_ID = "connectedUserId";

    private final AppMemberService appMemberService;

    public AuthController(AppMemberService appMemberService) {
        this.appMemberService = appMemberService;
    }

    @GetMapping("/login")
    public String loginPage() {
        return "login";
    }

    @PostMapping("/login")
    public String login(
            @RequestParam("email") String email,
            @RequestParam("password") String password,
            HttpSession session,
            RedirectAttributes redirectAttributes) {
        AppMemberEntity appMember = appMemberService.authenticate(email, password);
        if (appMember == null) {
            redirectAttributes.addFlashAttribute("errorMessage", "Connexion impossible");
            return "redirect:/login";
        }

        session.setAttribute(SESSION_USER_ID, appMember.getId());
        return "redirect:/me/favorites";
    }

    @GetMapping("/register")
    public String registerPage() {
        return "register";
    }

    @PostMapping("/register")
    public String register(
            @RequestParam("fullName") String fullName,
            @RequestParam("email") String email,
            @RequestParam("password") String password,
            RedirectAttributes redirectAttributes) {
        try {
            appMemberService.registerMember(fullName, email, password);
            redirectAttributes.addFlashAttribute("message", "Compte cree");
            return "redirect:/login";
        } catch (IllegalArgumentException exception) {
            redirectAttributes.addFlashAttribute("errorMessage", exception.getMessage());
            return "redirect:/register";
        }
    }

    @PostMapping("/logout")
    public String logout(HttpSession session, RedirectAttributes redirectAttributes) {
        session.invalidate();
        redirectAttributes.addFlashAttribute("message", "Deconnexion reussie");
        return "redirect:/login";
    }
}
```

### 21.13 页面 Controller

```java
@Controller
@RequestMapping("/movies")
public class StoredMovieController {

    public static final String SESSION_USER_ID = "connectedUserId";

    private final RemoteMovieService remoteMovieService;
    private final StoredMovieService storedMovieService;
    private final MemberMoviePreferenceService memberMoviePreferenceService;
    private final AppMemberService appMemberService;

    public StoredMovieController(
            RemoteMovieService remoteMovieService,
            StoredMovieService storedMovieService,
            MemberMoviePreferenceService memberMoviePreferenceService,
            AppMemberService appMemberService) {
        this.remoteMovieService = remoteMovieService;
        this.storedMovieService = storedMovieService;
        this.memberMoviePreferenceService = memberMoviePreferenceService;
        this.appMemberService = appMemberService;
    }

    @GetMapping
    public String movieList(@RequestParam(defaultValue = "1") Integer page, Model model) {
        RemoteMoviePageDTO remoteMoviePage = remoteMovieService.getPopularMovies(page);
        model.addAttribute("moviePage", remoteMoviePage);
        model.addAttribute("movies", remoteMoviePage.getResults());
        return "movie-list";
    }

    @GetMapping("/{movieId}")
    public String movieDetail(@PathVariable Long movieId, Model model) {
        model.addAttribute("movie", remoteMovieService.getMovieById(movieId));
        return "movie-detail";
    }

    @PostMapping("/{movieId}/favorite")
    public String addFavorite(@PathVariable Long movieId, HttpSession session) {
        AppMemberEntity appMember = getConnectedUser(session);
        if (appMember == null) {
            return "redirect:/login";
        }
        memberMoviePreferenceService.addFavorite(appMember.getEmail(), movieId);
        return "redirect:/movies/" + movieId;
    }

    @PostMapping("/{movieId}/favorite/remove")
    public String removeFavorite(@PathVariable Long movieId, HttpSession session) {
        AppMemberEntity appMember = getConnectedUser(session);
        if (appMember == null) {
            return "redirect:/login";
        }
        memberMoviePreferenceService.removeFavorite(appMember.getEmail(), movieId);
        return "redirect:/movies/" + movieId;
    }

    @PostMapping("/{movieId}/watchlist")
    public String addWatchlist(@PathVariable Long movieId, HttpSession session) {
        AppMemberEntity appMember = getConnectedUser(session);
        if (appMember == null) {
            return "redirect:/login";
        }
        memberMoviePreferenceService.addWatchlist(appMember.getEmail(), movieId);
        return "redirect:/movies/" + movieId;
    }

    @PostMapping("/{movieId}/watchlist/remove")
    public String removeWatchlist(@PathVariable Long movieId, HttpSession session) {
        AppMemberEntity appMember = getConnectedUser(session);
        if (appMember == null) {
            return "redirect:/login";
        }
        memberMoviePreferenceService.removeWatchlist(appMember.getEmail(), movieId);
        return "redirect:/movies/" + movieId;
    }

    @GetMapping("/stored")
    public String storedMovies(@RequestParam(required = false) String keyword, Model model) {
        model.addAttribute("storedMovies", storedMovieService.searchByTitle(keyword));
        model.addAttribute("keyword", keyword);
        return "stored-movie-list";
    }

    private AppMemberEntity getConnectedUser(HttpSession session) {
        Object userId = session.getAttribute(SESSION_USER_ID);
        if (userId == null) {
            return null;
        }
        return appMemberService.findById((Long) userId);
    }
}
```

### 21.13.1 用户空间 Controller

```java
@Controller
@RequestMapping("/me")
public class MemberSpaceController {

    public static final String SESSION_USER_ID = "connectedUserId";

    private final MemberMoviePreferenceService memberMoviePreferenceService;
    private final AppMemberService appMemberService;

    public MemberSpaceController(
            MemberMoviePreferenceService memberMoviePreferenceService,
            AppMemberService appMemberService) {
        this.memberMoviePreferenceService = memberMoviePreferenceService;
        this.appMemberService = appMemberService;
    }

    @GetMapping("/favorites")
    public String favorites(Model model, HttpSession session) {
        AppMemberEntity appMember = getConnectedUser(session);
        if (appMember == null) {
            return "redirect:/login";
        }
        model.addAttribute(
                "favoritePreferences",
                memberMoviePreferenceService.findFavoritePreferences(appMember.getEmail()));
        return "my-favorites";
    }

    @GetMapping("/watchlist")
    public String watchlist(Model model, HttpSession session) {
        AppMemberEntity appMember = getConnectedUser(session);
        if (appMember == null) {
            return "redirect:/login";
        }
        model.addAttribute(
                "watchlistPreferences",
                memberMoviePreferenceService.findWatchlistPreferences(appMember.getEmail()));
        return "my-watchlist";
    }

    private AppMemberEntity getConnectedUser(HttpSession session) {
        Object userId = session.getAttribute(SESSION_USER_ID);
        if (userId == null) {
            return null;
        }
        return appMemberService.findById((Long) userId);
    }
}
```

### 21.14 REST Controller

```java
@RestController
@RequestMapping("/api/movies")
public class MovieRestController {

    private final RemoteMovieService remoteMovieService;

    public MovieRestController(RemoteMovieService remoteMovieService) {
        this.remoteMovieService = remoteMovieService;
    }

    @GetMapping("/{movieId}")
    public RemoteMovieDTO getMovie(@PathVariable Long movieId) {
        return remoteMovieService.getMovieById(movieId);
    }
}
```

### 21.15 `TP4` 路径说明：Session 优先

默认按老师 `TP4` 要求时：

- 密码加密靠 `spring-security-crypto`
- 登录校验靠 `AppMemberService.authenticate(...)`
- 登录状态靠 `HttpSession`
- 不需要为了加密专门写 `SecurityFilterChain`

如果题目明确要求 `Spring Security` 登录保护，再参考 `TP6` 路径补 `starter-security`、`SecurityConfig`、`UserDetailsService`。

### 补充：默认用户初始化模板

```java
@Configuration
public class DataInitializer {

    @Bean
    public CommandLineRunner initializeDefaultUser(
            AppMemberRepository appMemberRepository,
            PasswordEncoder passwordEncoder,
            @Value("${app.security.default-user.email}") String defaultEmail,
            @Value("${app.security.default-user.password}") String defaultPassword) {
        return args -> {
            if (!appMemberRepository.existsByEmail(defaultEmail)) {
                AppMemberEntity appMember = new AppMemberEntity();
                appMember.setFullName("Admin");
                appMember.setEmail(defaultEmail);
                appMember.setPasswordHash(passwordEncoder.encode(defaultPassword));
                appMemberRepository.save(appMember);
            }
        };
    }
}
```

### 21.16 核心模板页

#### `login.html`

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
    <h1>Login</h1>
    <p th:if="${errorMessage}" th:text="${errorMessage}"></p>
    <p th:if="${message}" th:text="${message}"></p>
    <form th:action="@{/login}" method="post">
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
    </form>
    <a th:href="@{/register}">Creer un compte</a>
</body>
</html>
```

#### `register.html`

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
    <h1>Register</h1>
    <p th:if="${errorMessage}" th:text="${errorMessage}"></p>
    <form th:action="@{/register}" method="post">
        <input type="text" name="fullName" placeholder="Full name" required>
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Creer le compte</button>
    </form>
    <a th:href="@{/login}">Retour login</a>
</body>
</html>
```

#### `movie-list.html`

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
    <h1>Movies</h1>
    <div>
        <a th:href="@{/movies/stored}">Films sauvegardes</a>
        <a th:href="@{/me/favorites}">Mes favoris</a>
        <a th:href="@{/me/watchlist}">Ma watchlist</a>
        <form th:action="@{/logout}" method="post" style="display:inline;">
            <button type="submit">Logout</button>
        </form>
    </div>
    <table border="1">
        <tr th:each="movie : ${movies}">
            <td th:text="${movie.title}"></td>
            <td th:text="${movie.release_date}"></td>
            <td>
                <a th:href="@{/movies/{movieId}(movieId=${movie.id})}">Voir</a>
            </td>
        </tr>
    </table>
</body>
</html>
```

#### `movie-detail.html`

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
    <h1 th:text="${movie.title}"></h1>
    <p th:text="${movie.overview}"></p>
    <form th:action="@{/movies/{movieId}/favorite(movieId=${movie.id})}" method="post">
        <button type="submit">Ajouter aux favoris</button>
    </form>
    <form th:action="@{/movies/{movieId}/favorite/remove(movieId=${movie.id})}" method="post">
        <button type="submit">Retirer des favoris</button>
    </form>
    <form th:action="@{/movies/{movieId}/watchlist(movieId=${movie.id})}" method="post">
        <button type="submit">Ajouter a la watchlist</button>
    </form>
    <form th:action="@{/movies/{movieId}/watchlist/remove(movieId=${movie.id})}" method="post">
        <button type="submit">Retirer de la watchlist</button>
    </form>
    <a th:href="@{/movies}">Retour liste</a>
</body>
</html>
```

#### `stored-movie-list.html`

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
    <h1>Stored movies</h1>
    <form th:action="@{/movies/stored}" method="get">
        <input type="text" name="keyword" th:value="${keyword}" placeholder="Titre">
        <button type="submit">Search</button>
    </form>
    <table border="1">
        <tr th:each="movie : ${storedMovies}">
            <td th:text="${movie.title}"></td>
            <td th:text="${movie.releaseDate}"></td>
            <td th:text="${movie.voteAverage}"></td>
        </tr>
    </table>
    <a th:href="@{/movies}">Retour movies</a>
</body>
</html>
```

#### `my-favorites.html`

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
    <h1>Mes favoris</h1>
    <table border="1">
        <tr th:each="preference : ${favoritePreferences}">
            <td th:text="${preference.storedMovie.title}"></td>
            <td>
                <a th:href="@{/movies/{movieId}(movieId=${preference.storedMovie.id})}">Voir</a>
            </td>
        </tr>
    </table>
    <a th:href="@{/movies}">Retour movies</a>
</body>
</html>
```

#### `my-watchlist.html`

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
    <h1>Ma watchlist</h1>
    <table border="1">
        <tr th:each="preference : ${watchlistPreferences}">
            <td th:text="${preference.storedMovie.title}"></td>
            <td>
                <a th:href="@{/movies/{movieId}(movieId=${preference.storedMovie.id})}">Voir</a>
            </td>
        </tr>
    </table>
    <a th:href="@{/movies}">Retour movies</a>
</body>
</html>
```

### 21.17 最小可运行链路

如果时间很紧，最低限度先保证这 6 步：

1. `/login` 能进
2. `/register` 能注册
3. `/movies` 能显示 API 数据
4. `/movies/{id}` 能显示详情
5. 点收藏能把电影先存进数据库
6. 用户-电影关系能存进关系表

## 22. 常用注解速查表

### Spring Boot / MVC

- `@SpringBootApplication`
- `@Controller`
- `@RestController`
- `@RequestMapping`
- `@GetMapping`
- `@PostMapping`
- `@RequestParam`
- `@PathVariable`
- `@ModelAttribute`
- `@Service`
- `@Repository`
- `@Configuration`
- `@Bean`

### JPA

- `@Entity`
- `@Table`
- `@Id`
- `@GeneratedValue`
- `@Column`
- `@OneToMany`
- `@ManyToOne`
- `@ManyToMany`
- `@JoinColumn`
- `@JoinTable`

## 23. 常用函数模板汇总

### 查全部

```java
public List<AppMemberEntity> findAll() {
    return appMemberRepository.findAll();
}
```

### 按 ID 查

```java
public AppMemberEntity findById(Long memberId) {
    return appMemberRepository.findById(memberId).orElse(null);
}
```

### 新增

```java
public AppMemberEntity create(AppMemberEntity appMember) {
    return appMemberRepository.save(appMember);
}
```

### 修改

```java
public AppMemberEntity update(AppMemberEntity appMember) {
    return appMemberRepository.save(appMember);
}
```

### 删除

```java
public void deleteById(Long memberId) {
    appMemberRepository.deleteById(memberId);
}
```

### 搜索

```java
public List<StoredMovieEntity> searchByTitle(String keyword) {
    return storedMovieRepository.findByTitleContainingIgnoreCase(keyword);
}
```

### 注册

```java
@PostMapping("/register")
public String register(
        @RequestParam String fullName,
        @RequestParam String email,
        @RequestParam String password) {
    appMemberService.registerMember(fullName, email, password);
    return "redirect:/login";
}
```

### API 调用

```java
public RemoteMovieDTO getMovieById(Long movieId) {
    String url = movieApiBaseUrl + "/movie/" + movieId + "?api_key=" + movieApiKey;
    return restTemplate.getForObject(url, RemoteMovieDTO.class);
}
```

### DTO 转 Entity

```java
public StoredMovieEntity convertToEntity(RemoteMovieDTO remoteMovieDTO) {
    StoredMovieEntity storedMovie = new StoredMovieEntity();
    storedMovie.setId(remoteMovieDTO.getId());
    storedMovie.setTitle(remoteMovieDTO.getTitle());
    storedMovie.setReleaseDate(remoteMovieDTO.getRelease_date());
    storedMovie.setVoteAverage(remoteMovieDTO.getVote_average());
    storedMovie.setOverview(remoteMovieDTO.getOverview());
    return storedMovie;
}
```

### 页面传值

```java
model.addAttribute("movies", remoteMoviePage.getResults());
model.addAttribute("storedMovies", storedMovieService.findAll());
model.addAttribute("favoritePreferences", memberMoviePreferenceService.findFavoritePreferences(email));
```

### 重定向

```java
return "redirect:/movies";
```

### Flash 消息

```java
redirectAttributes.addFlashAttribute("message", "Operation reussie");
redirectAttributes.addFlashAttribute("errorMessage", "Erreur de validation");
return "redirect:/register";
```

### Session 取当前用户

```java
public String favorites(Model model, HttpSession session) {
    Object userId = session.getAttribute("connectedUserId");
    if (userId == null) {
        return "redirect:/login";
    }
    AppMemberEntity appMember = appMemberService.findById((Long) userId);
    model.addAttribute(
            "favoritePreferences",
            memberMoviePreferenceService.findFavoritePreferences(appMember.getEmail()));
    return "my-favorites";
}
```

老师 `TP4` 要求下，更常用的是 `HttpSession`，不是 `Principal`。

### 收藏 / 取消收藏

```java
@PostMapping("/{movieId}/favorite")
public String addFavorite(@PathVariable Long movieId, HttpSession session) {
    AppMemberEntity appMember = getConnectedUser(session);
    memberMoviePreferenceService.addFavorite(appMember.getEmail(), movieId);
    return "redirect:/movies/" + movieId;
}

@PostMapping("/{movieId}/favorite/remove")
public String removeFavorite(@PathVariable Long movieId, HttpSession session) {
    AppMemberEntity appMember = getConnectedUser(session);
    memberMoviePreferenceService.removeFavorite(appMember.getEmail(), movieId);
    return "redirect:/movies/" + movieId;
}
```

### Watchlist

```java
@PostMapping("/{movieId}/watchlist")
public String addWatchlist(@PathVariable Long movieId, HttpSession session) {
    AppMemberEntity appMember = getConnectedUser(session);
    memberMoviePreferenceService.addWatchlist(appMember.getEmail(), movieId);
    return "redirect:/movies/" + movieId;
}
```
